import { renderWithContext } from '@utils/test/renderWithContext';
import AllTickets from '@organisms/allTickets';

describe('<AllTickets />', () => {
    it('rendering test', () => {
        renderWithContext(<AllTickets />);
        //TODO: rendering test with Mock Data
    });
});
